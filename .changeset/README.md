# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets). Add a
changeset for any user-facing change with `pnpm changeset`, then `pnpm release` builds and
publishes the `@kroszborg/*` packages (all versioned together).
